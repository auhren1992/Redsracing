# Local-only environment (never commit `.env`).
# Used by future scripts / tooling — Firebase web keys stay in `assets/js/firebase-config.js` for the client.

# Example placeholders:
# SENTRY_DSN=
# CUSTOM_API_KEY=
