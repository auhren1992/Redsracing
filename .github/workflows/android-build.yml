name: Android Build

on:
  push:
    branches: [main]
    paths:
      - 'android/**'
      - '.github/workflows/android-build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        working-directory: android
        run: chmod +x gradlew

      - name: Decode Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        working-directory: android
        run: |
          echo "$KEYSTORE_BASE64" | base64 -d > upload-keystore.jks

      - name: Build Release Bundle
        env:
          REDSRACING_KEYSTORE_PASSWORD: ${{ secrets.REDSRACING_KEYSTORE_PASSWORD }}
          REDSRACING_KEY_PASSWORD: ${{ secrets.REDSRACING_KEY_PASSWORD }}
        working-directory: android
        run: ./gradlew bundleRelease --no-daemon --stacktrace -PREDSRACING_KEYSTORE_PASSWORD=$REDSRACING_KEYSTORE_PASSWORD -PREDSRACING_KEY_PASSWORD=$REDSRACING_KEY_PASSWORD

      - name: Upload to Google Play (Internal Testing)
        env:
          GOOGLE_PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
        working-directory: android
        run: |
          # Create service account JSON file
          echo "$GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" > service-account.json
          
          # Upload to Google Play using Gradle Play Publisher plugin
          ./gradlew publishBundle \
            --track=internal \
            -Pplay.serviceAccountCredentials=service-account.json \
            --no-daemon \
            --stacktrace
          
          # Clean up service account file
          rm service-account.json

      - name: Upload Bundle Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-bundle
          path: android/app/build/outputs/bundle/release/*.aab
          retention-days: 30

      - name: Clean up keystore
        if: always()
        working-directory: android
        run: rm -f upload-keystore.jks
